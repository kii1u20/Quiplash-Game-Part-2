<div v-if="state.state == 0">
    <button class="btn btn-lg btn-primary" @click="startGame()">Start Game</button>
</div>
<div v-else>
    <button class="btn btn-lg btn-primary" @click="next()">Next</button>
</div>
